# Marketing ROI - Simple Linear Regression Analysis

## Overview
This project uses Python and `statsmodels` to build a Simple Linear Regression model analyzing the relationship between marketing expenditures (TV, Radio, Social Media) and Sales.

## Environment Setup
To run this notebook locally, install the required dependencies:
```bash
pip install pandas seaborn statsmodels matplotlib numpy
project
